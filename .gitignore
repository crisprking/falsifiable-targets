# generated figure output (fig_boundary_surface.py writes this)
fig_boundary_surface.png
*.png

# python
__pycache__/
*.pyc
.ipynb_checkpoints/

# OS
.DS_Store

# NOTE: the saved audit JSONs (scope_rule_grid.json, mapping_audit.json,
# panel_bootstrap.json, indication_anchored.json) are NOT ignored — commit them
# alongside a pinned Open Targets release if you want the figures reproducible
# without re-querying.
